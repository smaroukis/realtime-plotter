// USE: change the file name to `secrets.h` and add `include/secrets.h` to your `.gitignore`

#ifndef SECRETS_H
#define SECRETS_H

#define SECRET_SSID "<wifi_ssid>"   // your network SSID (name)
#define SECRET_PASS "<wifi_password>"  // your network password (use for WPA, or use as key for WEP)
#define MQTT_SERVER "<loacl_mqtt_broker_ip" // mqtt server e.g. 192.168.132.123
#define MQTT_PORT 1883

#endif
